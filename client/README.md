# BookStore Client

This is my BookStore client project.

## How to run

1. Install the packages with `npm install`.
2. Start the app with `npm run dev`.

I built this with React and Vite for my MERN BookStore project.
